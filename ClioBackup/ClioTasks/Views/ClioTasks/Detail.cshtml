@model  ClioTasks.Models.ClioTask 
@{
    ViewBag.Title = "Detail";
}

<h2>Index</h2>

<table class="table">
    <tr>
        <th>
            Subject
        </th>
        <th>
            Created
        </th>
        <th>
            Due date
        </th>
        <th>Edit</th>
        <th>Delete</th>
    </tr>
    
        <tr>
            <td>
                @Html.DisplayFor(modelItem => Model.name)
            </td>
            <td>
                @*@Html.DisplayFor(modelItem => item.CreatedDateTime)*@
            </td>
            <td>
                @*  @Html.DisplayFor(modelItem => item.DueDateTime.DateTime)*@
            </td>
            <td>
                @*@Html.ActionLink("Edit", "EditTask", "Home", new { Id = item.Id }, new { @class = "ui-btn-right", data_icon = "gear" })*@
            </td>
            <td>
                @*@Html.ActionLink("Delete", "DeleteTask", "ClioTasks", new { Id = Model.id }, new { @class = "ui-btn-right", data_icon = "gear" })*@
            </td>
        </tr>
   
</table>